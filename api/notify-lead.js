export default async function handler(req, res) { if (req.method !== "POST") return res.status(405).end(); const { businessName, name, email, phone, businessType, teamSize, bookingMethod, notes } = req.body; await fetch("https://api.resend.com/emails", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": `Bearer ${process.env.RESEND_KEY}` }, body: JSON.stringify({ from: "The Booking Plug <noreply@thebookingplug.net>", to: "nconda05@gmail.com", subject: `New Lead: ${businessName}`, html: `<h2>New Lead</h2><p><b>Business:</b> ${businessName}</p><p><b>Name:</b> ${name}</p><p><b>Email:</b> ${email}</p><p><b>Phone:</b> ${phone}</p>` }) }); res.status(200).json({ success: true }); }
